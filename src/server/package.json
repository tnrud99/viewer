{
  "name": "ve-url-server",
  "version": "1.0.0",
  "description": "Server for VE URL system",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "build": "echo 'Build completed'",
    "dev": "nodemon server.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "build:editor": "node ../../scripts/build-editor.js",
    "build:viewer": "node ../../scripts/build-viewer.js",
    "build:all": "npm run build:editor && npm run build:viewer",
    "validate:components": "npm run build:all"
  },
  "keywords": [
    "ve-url",
    "reaction-video",
    "synchronization",
    "express",
    "mongodb"
  ],
  "author": "Your Name",
  "license": "MIT",
  "dependencies": {
    "express": "^4.18.2",
    "mongoose": "^7.5.0",
    "cors": "^2.8.5",
    "jsonwebtoken": "^9.0.2",
    "bcryptjs": "^2.4.3",
    "multer": "^1.4.5-lts.1",
    "dotenv": "^16.3.1"
  },
  "devDependencies": {
    "nodemon": "^3.0.1"
  },
  "engines": {
    "node": "22.x"
  }
} 